# D — consistent backup and rollback proof

Problem: the release runbook references a nonexistent consistent backup procedure. A copied SQLite main file may omit WAL transactions, and a code rollback must never delete the newer ledger/research evidence.

Evidence: Compose uses trading_data at /data for engine and gateway, agent reads it read-only; default research archive is /data/research. Actual volume identity, overridden paths and all external consumers must be inspected at release time. This slice runs only Dev fixtures, never Production stops/backups/restores.

Files/contracts: new scripts/verify_data_backup.py and focused tests; backup/rollback runbook linked from deployment-verification-runbook; guide/atlas/next-plan/checklist. The tool verifies a supplied full-tree tar backup, not live quiescence or profitability. No trading/accounting mutation.

Steps: specify exclusive after-hours operator window; record release/images and exact live mounts; halt autoheal/restart automation and every data writer with authorization; prove all consumers stopped before and after archive capture; capture complete tree including WAL/SHM, selected-leg/attempt journals, raw masters and research manifests. Verify tar safety/duplicate paths/bounded extraction, per-file inventory/hash and SQLite integrity using isolated scratch extraction with WAL companions. Refuse overwriting receipts and reject malformed/corrupt/missing/unsafe archives. Preserve source volume and all post-backup data; restore only into a new isolated volume after separate authorization. Test old/new code schema/archive compatibility before any rollback.

Acceptance: fixture contains uncheckpointed WAL transaction and multiple research files; full-tree archive restores correct rows and hashes. Missing WAL/corruption/tamper/path traversal/duplicate members/link/device/oversized archive fail or explicitly remain unverifiable. Verification cannot label backup consistent without an externally retained quiescence receipt; document that distinction. Receipt links archive digest, inventory and SQLite observations. No production credentials/content in test logs.

Migration/config: none; new optional offline operator verifier. Rollback: revert tool/runbook source through GitHub; retain all backups/operational data. Status IMPLEMENTING. Documentation updated with source commit and immediately reconciled afterward.

Unresolved: actual operator-authorized backup, independent live quiescence and restore rehearsal on actual data, release PR/deployment/session evidence and E–J. Next action: implement bounded offline backup verifier and negative tests while independently reviewing writer/rollback assumptions.

Read-only runtime discovery, September 13: exact existing `/data` volume is `production_trading-sentinel_trading_data`; engine/gateway mount read-write, agent read-only. All three are currently stopped with exit137, Docker OOMKilled=false, finished around 01:01 UTC; autoheal/nginx/ngrok are running. These stopped states are not a maintenance authorization or proof of exclusive quiescence; restart automation and external writers remain uncontrolled. Root did not stop/restart these services or modify Production. Asked user asynchronously whether stoppage was deliberate; safe Dev work continues. Production checkout remains evolve/smart-strategies with existing untracked audit docs/migration preserved.

Verification: final six-file Windows backup/deployment/performance/attempt/selected-leg/capture group with `-q -W error`: 117 passed/one symlink-privilege skip/no warnings in 5.01s; backup/deployment subset 29 passed/one skip. Source fixture keeps WAL connection open through capture and verification, asserts recovered row and byte-identical source. Independent/root review and added tests cover actual tar member rejection, matching corrupt DB integrity failure, enumeration/expansion bounds, Windows parent aliases, changing archive/source and exclusive receipts independent of optional symlink privileges. Native dummy-data inventory/Alpine-tar/verify helper commands return0 using cached network-disabled images; fixture/receipt retained in user Temp directory as recorded in checklist. Engine image has no pytest, so no false cross-platform suite claim. Compilation/diff checks pass and atlas regenerated unchanged (155 modules).

Status: tool/runbook slice TESTED_DEV pending commit identity; overall D IMPLEMENTING. No runtime flags/schema/positions/claims or Production operations changed. Scope limits: tool validates header-identified SQLite and file hashes, not completeness of an independently unaudited source inventory or cross-file live consistency. Real exclusive quiescence, actual data restore/previous-code compatibility and release remain required. Next action: review Dev release diff/defaults and prepare reviewed GitHub PR while obtaining actual maintenance/backup prerequisites. No push/deployment or actual message/order performed.
