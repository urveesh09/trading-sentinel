# Independent verification of external F–J implementation

Scope: user asks to verify the other agent's F2–F6/H/I/J commits, correct defects and finish the current G/C workflow. Dev HEAD7474199 at start;68 commits/145 changed files since root1a0f483. Preserve all external commits and two dirty generated-at golden fields. No Production edits or deployment. Assigned F and execution-boundary auditors work read-only; root supervises and independently reproduces findings. After explicitly interrupting the pending protocol agent, root owns G/C source and independent acceptance tests.

Acceptance basis: original NEXT_AGENT_PLAN workstream deliverables, actual owning schemas/callers and official primary sources for session rules. Test-count/done-docs claims are not acceptance. Full-engine, native gateway, dashboard and isolated agent acceptance must be rerun after reviewed corrections. Actual operator-approved capital, external reconciliation, staging broker behavior, release and genuine market sessions remain separate requirements.

Current reproduced G defects: root actual-simulator `tests/test_comparison_protocol_acceptance.py -q -W error` returns2failed/1pass0.70s: profile supports further research despite missing declared holdout session; identical old freeze retry afterholdout fails. Full cost metadata binding fixture passes. Root CLI pair returns7pass/no warnings0.75s. Corrections and full protocol/gate/uncertainty review in progress; cbaff17's protocol-complete claim is unaccepted.

Preliminary F findings (independent auditor source review, root spot-check): F&O stored qty is already lots*lot_size, but mark_to_market multiplies qty by lot_size again (1lot/50qty/premium100→110 yields25000 instead of500). Defined-risk leg JSON schema also differs from valuation's expected adapter fields; require actual owning-writer fixtures. Capital-increase policy accepts non-positive requested_delta, mixes source/account evidence and defaults an unprovided user loss tolerance25%; audit must determine consumer authority and repair exact scope. These are open defects, not proof the full audit is finished. Do not repair operational books to agree.

Next order: finish/review G/C protocol and retained comparison, then implement independently reproduced F corrections with owning-source regressions; incorporate H/I/J audit findings, rerun complete acceptance and reconcile all canonical docs immediately at commits. Lowest-overlap external assignment: E preview/card clarity/operator suppression explanations only, with no shared schemas/scheduling/qualification/delivery authority changes.

## Completed read-only reviews (not corrected/accepted yet)

F auditor:197 focused tests pass with warnings fatal; reconciliation route tests16 pass with17 dependency deprecations. These tests miss the actual writer quantity/schema defects. F4 keys `(category,evidence_key)` omit account and duplicate ingestion can return another account's discrepancy; local accountless facts must not be labeled broker-account-linked. `record_current_state` writes separate evidence pieces, not one atomic snapshot. F5 repeated immutable output differs on `imported` and raises uncaught ValueError; null IDs stringify to `None`. Explicit statement import writes its dedicated tables and must not be described as read-only.

F6 wrapper uses hardcoded EDGE_LIVE allocation, accountless quality/loss history, arbitrary nonempty archive files and failure-open defaults. Its sole consumer is the operator CLI, not a broker executor: AUTHORIZED currently is an evaluation label, not effective order authority. The claimed user25% loss tolerance was not supplied; preserve unknown/refusal until explicit input. F2 seam passes pending_live_growth=0 in both orchestrators; do not claim the growth guard is enforced by those no-op calls.

J/H auditor:115 Python and68 Node focused tests pass but actual Node entry/callback omit CAS eligibility and default configured eligible stocks to continuous trading. Momentum15:15 square-off cancels its stop then submits a normal LIMIT without a session gate/deadline; monitor can race that owner. Derivative boolean conflates futures/options and exchange price-band scope with execution permission. Holiday drift tool reports Python20/Node18 dates with only10 overlap; degraded boot uses mismatched fallback. Reachability gate trusts supplied phase strings, while its documented probe forces cash instruments and cannot produce required derivative captures. Golden explicit-eligibility vectors do not exercise these live callers.

Primary exchange references used by auditor: [NSE CAS timetable](https://www.nseindia.com/static/products-services/closing-auction-session), [NSE futures CAS SOP](https://nsearchives.nseindia.com/content/circulars/CMTR76170.pdf), [NSE holiday API](https://www.nseindia.com/api/holiday-master?type=trading). Correct coarse labels conservatively or model seconds/random-close uncertainty explicitly; never infer permission from a price-band phase. Independent correction designs assigned read-only pending current G commit reconciliation. I remains unaudited. These findings supersede external F-series/J closure labels.
