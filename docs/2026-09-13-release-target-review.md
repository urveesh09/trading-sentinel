# D — refreshed release target and default review

Scope: read-only review of Dev `0f3a2a0` against freshly fetched GitHub target `954e25abe588db3f1b560069661569cd93aa8742` (PR88 merge). No push, merge, deployment, Production edit or service restart. Fetch updates remote-tracking references only.

Use `git diff origin/evolve/smart-strategies HEAD` for endpoint behavior review. It contains 74 changed files, 7,100 insertions and 167 deletions at this review identity. Target is not an ancestor of Dev: historical merge commits remain on the target side; merge-base is `841131e274be43bb92ae7270a51cb4fd7ec1bea1`. A triple-dot PR comparison includes already released history and is not the actual endpoint release delta. Reconcile this history non-destructively and re-run acceptance before submitting the final PR; do not reset or force-push user history.

Root and independent Luna review: Dockerfiles, Compose and `performance.py` are byte-identical to the refreshed target (`git diff --quiet origin/evolve/smart-strategies HEAD -- docker-compose.yml agent/Dockerfile node-gateway/Dockerfile python-engine/Dockerfile python-engine/performance.py` returns0). Existing `trading_data` mounts remain engine/gateway RW and agent RO. Actual Production-prefixed volume identity remains separate from the Compose logical name.

Only behavioral configuration addition: `RESEARCH_CAPTURE_WAIT_TIMEOUT_SEC=2.0`; shielded archive writes can finish after timeout, whose outcome remains explicitly unknown. Observe late completion, contention and retention during the genuine session. The partner-bot comment correction does not change its value.

Existing target and Dev both default `PARTNER_BOT_ENABLED=True` and manual-advisory delivery switches True. They are NOT disabled-by-default. Effective rollout configuration, destinations and downstream qualification/session gates must be explicitly verified before service startup; a passive-source release must not imply permission to send messages. No actual transport test or order is authorized by this review. Existing penny/FNO live-disable gates remain disabled for live trading; conditional hedge/input refresh remain off. Passive research collection remains enabled and source-only.

Final complete-suite receipt: from Dev `python-engine`, `.\winvenv\Scripts\python.exe -m pytest tests -q --junitxml=C:/Users/Urveesh/AppData/Local/Temp/sentinel-d-final-baseline-20260913.xml` returns0: **2,569 passed, four skipped, 23 existing Starlette/httpx deprecation warnings in 121.31s**. No exclusions or new warning suppression. This includes the backup verifier tests and test-loop cleanup after the earlier 2,545-pass baseline. No runtime source edits during this rerun.

Remaining: previous-code compatibility on isolated retained data copies, authorized actual backup/restore, reviewed GitHub promotion, live identity and real-session/held-out evidence; E–J retain their own acceptance. Next action: reconcile release ancestry without losing user commits, then prepare PR with exact endpoint behavior, verification and explicit delivery/source rollout switches. Do not infer maintenance authorization from stopped containers.
