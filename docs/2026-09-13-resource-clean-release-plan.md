# D — resource-clean release prerequisites

Problem: green acceptance still exposes a gateway token-restore abort timer after failed fetch and real fake-token Telegram polling from a filesystem-only test. The combined Python warning-fatal suite also reports an unclosed Windows asyncio socket. Resource ownership must be diagnosed, not hidden with warning filters or forced exits.

Evidence: baseline commit f7e33cb/docs dcc139e; full Python 2,545 passes/three skips; native gateway 318 passes/four skips with two detected open handles. Existing defaults/order/advisory authority remain unchanged.

Affected files/contracts: gateway token-restore request/response timeout ownership, its unit tests, filesystem-only alert-dead-letter test transport stub. Python loop ownership changes only after independent causal evidence. Backup/rollback runbook reviewed separately, with no direct Production operations.

Implementation: release per-attempt abort timer in finally on every path; retain its bound through response-body parsing so a stalled body is abortable. Preserve retries, internal auth, disarmed failure and token freshness policy. Stub Telegram construction in the filesystem-only test so no polling is started. Verify timeout cleanup for fetch rejection, non-2xx, malformed body, successful/no-token responses and an abort-aware stalled response. Run gateway full native suite without forceExit and with detectOpenHandles after network disconnection. Investigate Python warning with allocation tracing and loop-owner review before any change.

Acceptance: explicit regression fails on original timer/body behavior and passes after fix; native gateway terminates naturally without detected handles; Python warning root cause retained and independently checked. No actual network advice/orders, qualification, flags or capital changes. Tests use scratch containers/temporary databases only.

Migration/config: none. Rollback: revert bounded source commit in Dev, retain operational data. GitHub promotion and stamped release verification remain separate. Status IMPLEMENTING; guide/atlas/plan/checklist updated before commit and reconciled immediately afterward.

Remaining: consistent backup/restore proof, reviewed PR/release and real market-session/held-out evidence, then independent E–J workstreams. Next action: fix gateway ownership with negative tests while Terra investigates Python loop ownership.

Python discovery: Terra reproduced the two-file sequence with `-W error`; each module passes alone, and excluding only `test_option_lookup` removes the failure. That sync test manually calls asyncio.run between pytest-managed async tests, abandoning the previously installed pytest loop on Windows. Convert it to the same pytest-managed async lifecycle as adjacent tests, without global loop fixtures or warning suppression. Root verifies the original two/four-file sequence and broad research group.

Release discovery: deployment-verification-runbook refers to a consistent backup 'described above', but contains no actual backup procedure. Inventory existing backup tooling, data/SQLite/archive mount and writer scope before specifying quiescence/verification/restore steps; do not run any Production stops or backup writes in this slice.

Verification (September 13, Dev): `winvenv\Scripts\python.exe -m pytest tests/test_fno_instruments.py tests/test_fno_signal_scan.py tests/test_partner_full_policy_replay.py tests/test_partner_full_policy_multisession.py -q -W error` => 32 passed/no warnings, 3.41s. Broad 15-file research group (these four plus qualification/review/capture/CLI/signal-artifact/research/replay/holdout/chronological/archive-adapter/orchestrator) with `-W error::ResourceWarning -W error::pytest.PytestUnraisableExceptionWarning` => 181 passed/one existing Starlette deprecation, 7.83s. No warning filter suppression added.

Native gateway: node:20-alpine fb4cd12c85ee, npm ci in source copy excluding node_modules/.env/data, bridge disconnected, recopy stable current Dev source, `./node_modules/.bin/jest --runInBand --detectOpenHandles` => 324 passed/four skipped, 25 suites passed/one skipped, 16.927s, natural exit 0, no detected handles. Focused token/dead-letter command with same options => 15 passed, 0.483s, natural exit. Negative controls in scratch copy only: original HEAD token-restore + rejection regression => expected failure (one timer); original + body regression with 500ms Jest timeout => expected timeout failure. Restored current source afterward. Logs and removed scratch helper/container are recorded in checklist. Existing package script's forceExit was not used. Root reviewed Luna's finally scope and ensured fake timers restored before assertion. JS syntax/Python compilation/diff checks pass; atlas regenerated unchanged at 155 modules.

Status: bounded resource slice TESTED_DEV pending commit identity. No migration/config/order/delivery permission changes. Token response body can now be aborted at the existing three-second bound rather than wait indefinitely. Overall D remains IMPLEMENTING. Next executable action: inventory the actual named-volume and external archive writers, specify and verify a quiescent consistent backup/restore in Dev before authorized release operations. No Production edits/stops, push, deploy, messages or orders in this slice.
