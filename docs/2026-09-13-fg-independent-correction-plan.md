# F/G — independent correctness audit and corrections

F correction full-engine acceptance: from Dev `python-engine`, `.\winvenv\Scripts\python.exe -m pytest tests -q --junitxml=C:/Users/Urveesh/AppData/Local/Temp/sentinel-fg-provenance-baseline-20260913.xml` returns0: **2,703 passed/four skipped/23 existing Starlette-httpx deprecations in126.20s**. Runtime/assertion source remained frozen; only explanatory F test docstring changed during run. This includes cache, budget and F corrections and supersedes previous whole-engine counts. Independent Terra confirms F source/docs correctness with the known route-import warning visible; no current F correctness blocker. Related G contract prose corrected to distinguish proposed gates from shipped persistence and broker imports from local matching. Slice TESTED_DEV pending commit; no deployed-data compatibility, real loss reconstruction, tariff-date provenance or genuine held-out approval claim.

F final verification scope: root eight-file cost/F1/proactive/bridge command `.\winvenv\Scripts\python.exe -m pytest tests/test_F_phase1_inventory.py tests/test_cost_schedules.py tests/test_proactive_intelligence.py tests/test_proactive_execution_research.py tests/test_proactive_exit_research.py tests/test_proactive_portfolio_research.py tests/test_trailing_stop_profile.py tests/test_promotion_bridge.py -q -W error` from Dev engine:150 pass/no warnings6.48s. Add `tests/test_broker_reconciliation.py tests/test_reconciliation_evidence.py` with `-q -W error::ResourceWarning -W error::pytest.PytestUnraisableExceptionWarning`:154 pass7.07s, one existing Starlette lifespan deprecation. Terra independently runs `tests/test_cost_schedules.py tests/test_F_phase1_inventory.py tests/test_broker_reconciliation.py -q -W default`:19 pass/one same warning and finds no F source/docs blocker. That three-file scope does not pass all-warning-fatal acceptance; no such claim is made. Compilation/atlas/diff check pass156 modules. Full suite running with runtime/assertion code frozen; F test explanatory docstring corrected during that run only, not assertions or executable behavior. Genuine evidence still missing; no Production/remote operation.

F scoped acceptance exposed an existing test bootstrap dependency: broker activity-route test imports `routes_commands` before `main`, triggering the main/router circular import when run alone; whole suite had already initialized main. Correct only that test to follow the application entrypoint import order, not runtime routing. Initial ten-file all-warning-fatal run:153 pass/one existing Starlette lifespan deprecation failure; resource-warning-fatal rerun:153 pass/one bootstrap ImportError/one existing deprecation. Record reruns honestly; no warning suppression or runtime refactor in this slice.

F provenance correction slice: restore `EQUITY_INTRADAY_EFFECTIVE_DATE=None` in `cost_schedules.py` and replace assertions equating verification with tariff effectiveness in both cost/F1 test files. Preserve schedule version, verified-as-of date, all numeric rates, namespace behavior, options metadata and existing immutable snapshots. Correct the F inventory's effective-date/closed-warning claims and canonical status matrix: no actual warning records/screenshots/statements have been supplied to prove A1–A5 shapes or closures. Independently audit owning source schemas before their census correction. Acceptance: both focused test files, all cost-consuming proactive comparison/replay tests, actual broker PK behavior; compilation/atlas/diff review and independent review. No DB mutation/schema/flags/order/transport/default change. Source rollback through GitHub only; previously persisted date claims remain historical evidence, not rewritten. Real dated component-rate provenance, forensic losses and reconciliation remain unfinished. Status IMPLEMENTING.

Approval-budget post-commit reconciliation: source `2cbe1a3`; immediate seven-file stat/status review confirms clean Dev and consistent canonical docs/atlas. Slice TESTED_DEV, full correction plan IMPLEMENTING. No Production mutation, push or deployment. This replaces its pending source identity; next work remains genuine comparison/evidence validation and accurate F provenance/schema/warning records.

User request: independently audit the parallel agent's F/G work and fix errors while preserving its changes. Terra reviewed the new F/G source/docs; root independently checked the bridge source, actual broker schema and 75 warning-fatal focused tests. Those tests pass but miss the reproduced behaviors below. Production untouched.

Confirmed defects, not accepted work: promotion bridge reads its immutable initial state, allowing UNSIGNED → REFUSED then another UNSIGNED → APPROVED_LIVE_BUDGET; current-state ordering uses caller timestamps with no append-order tie-break. Approvals can omit budget/DD/expiry, are not bound to validated held-out/account/F/D evidence, and reads do not expose expiry/stale versions. Selected trailing exit is ignored for NEXT_EXECUTABLE_OPEN and BOUNDED_PULLBACK entry profiles; confirmation+trailing also bypasses confirmation. RANGE_REVERSION still aliases confirmation and must not be described as faithful hypothesis evaluation.

F evidence corrections required: broker entries PK is `(account_id, statement_id, entry_id)`, not the documented pair; actual owner schemas must replace inferred architecture claims. Audit-era recollections A1–A5 do not establish closure. Setting tariff effective date equal to verification date lacks primary-source support; retain uncertainty or explicitly labeled assumption rather than claiming an exchange-effective date. Existing immutable artifacts must not be rewritten. External agent actively owns F: coordinate/correct bounded overlap carefully, never overwrite unrelated work.

First implementation slice: `promotion_bridge.py` and tests only, plus canonical documentation/atlas. Use directed transition pairs, BEGIN IMMEDIATE before latest-state check/append, and SQLite append rowid order for this existing append-only table. Preserve both tables and all rows; no destructive schema migration. Regression covers repeated/conflicting transitions, reverse transitions, simultaneous callers and equal/backdated clocks. Caller timestamps remain evidence, not authority to reorder committed history. Existing can_place_orders=False remains mandatory.

Acceptance: exactly one first terminal transition succeeds even with concurrent requests; terminal state cannot change, reads follow committed append order and repeated/unknown operations fail safely. Existing tests remain green, full relevant source regression after subsequent corrections. Rollback source through GitHub only; never restore older DB rows. Migration/config: none for first slice, SQLite rowid exists in current schema; no live approval/order/transport or deployment.

Verification first slice: from Dev `python-engine`, `.\winvenv\Scripts\python.exe -m pytest tests/test_promotion_bridge.py tests/test_range_reversion_profile.py tests/test_trailing_stop_profile.py tests/test_legacy_run_id_shim.py tests/test_session_phase_placeholder.py tests/test_F_phase1_inventory.py tests/test_cost_schedules.py -q -W error`: 81 passed/no warnings in 1.63s. Six new behavior regressions; original 75 passing tests were insufficient to prove state/dispatch fidelity. Root reviewed source diff; independent Terra approves the atomic/directed/append-order correction and independently reruns 36 bridge tests warning-free. Rejected validation rolls back on connection close, before any writes, retaining previous state. Compilation/diff checks pass, atlas regenerated at 156 modules. Existing-row preservation follows no UPDATE/DELETE/schema change; genuine backup compatibility remains D evidence, not this fixture claim.

Primary-source review for F date concern: [Zerodha October 2024 revision](https://zerodha.com/z-connect/business-updates/revision-in-exchange-transaction-charges-and-securities-transaction-tax-from-october-1-2024) explicitly gives its tariff effective date; [current charges](https://zerodha.com/charges) lists rates, not proof that the combined configured schedule took effect on 2026-08-10. A verification date alone cannot justify that effective-date assignment. No replacement effective date invented; retain uncertainty until all relevant rates/dates are sourced.

Status first terminal-state slice TESTED_DEV in source commit `13fb426`; immediate seven-file stat/status review confirms clean Dev and matching guide/atlas/plan. Local only, not pushed/deployed. Whole correction plan IMPLEMENTING. Remaining ordered corrections: immutable required budgets plus validated approval/evidence expiry/status; compositional entry/exit dispatch with causal limit/confirmation behavior; accurate F schema/provenance/warning evidence docs and tests; adequate predeclared G comparison evidence. Do not mark whole G or this audit complete after terminal-state fix. D/E/I continue independently; actual release/backup/session and genuine qualification still required.

Next slice — faithful trailing composition: modify only `proactive_intelligence.py` and `test_trailing_stop_profile.py` plus canonical docs/atlas. Route each entry policy to the selected trailing exit without replacing pullback with at-open or bypassing confirmation. Reuse the existing trailing evaluator with explicit bounded-limit fill semantics; confirmation first advances cutoff, then selects the exit evaluator. Limit entry cannot pay above its bound; gap-invalid geometry remains no-fill. For an intrabar limit fill, the full entry-bar high may precede fill, so don't use that unproven high to raise the next stop. Preserve stop-first adverse same-bar handling and post-entry bar causality, fees, deadline and no-execution authority. Acceptance: behavioral regressions across NEXT/LIMIT/CONFIRMATION, limit no-touch/gap/entry-high ambiguity, confirmation no-later-bar, invalid assumptions and positive costs; existing proactive/replay tests. No schema/default/flags changes, preserve archived results and new runs need distinct immutable identities. Rollback source only through GitHub; F files untouched. Status IMPLEMENTING, independent review after source tests before commit. This does not turn RANGE_REVERSION's documented alias into a faithful range hypothesis, or validate approval evidence.

Trailing verification: root from Dev engine `.\winvenv\Scripts\python.exe -m pytest tests/test_trailing_stop_profile.py tests/test_proactive_intelligence.py tests/test_proactive_execution_research.py tests/test_proactive_exit_research.py tests/test_proactive_portfolio_research.py tests/test_proactive_market_data.py tests/test_range_reversion_profile.py tests/test_promotion_bridge.py tests/test_legacy_run_id_shim.py tests/test_session_phase_placeholder.py -q -W error`: 121 passed/no warnings in 5.53s. Initial test run revealed three new assertions using nonexistent ShadowSimulation.exit_at; corrected to its actual closed-result last_bar_at without changing runtime fields. Terra independently approves causal composition and retained primary-comparison manifests; no blocker. Full engine `.\winvenv\Scripts\python.exe -m pytest tests -q --junitxml=C:/Users/Urveesh/AppData/Local/Temp/sentinel-fg-trailing-baseline-20260913.xml` returns0: 2,656 passed/four skipped/23 existing Starlette-httpx deprecations in 124.91s, source frozen during run. Compilation/atlas/diff checks pass, 156 modules. Trailing slice TESTED_DEV pending source commit; whole F/G correction plan remains IMPLEMENTING. No Production ops/creds/messaging or remote promotion.

New aligned discovery: primary `run_shadow_research_comparison` binds implementation SHA plus full proposal geometry/identity/clocks and bars, so prior runs fail closed on source change. The separate `proactive_exit_research.persist_exit_policy_comparison` already binds bars/costs, but only binds a four-value proposal tuple and no implementation digest; same ID can return old results after instrument/timing/implementation changes. Next identity slice must use full canonical proposals and reviewed implementation fingerprint/version, preserve old rows and require a new run ID on mismatch. Not repaired by this trailing dispatch slice; do not claim all comparison provenance accepted.

Post-commit reconciliation: trailing source `5bede72`, immediate seven-file stat/status confirms clean Dev and matching documentation. All tested source unchanged at commit; local only, not pushed/deployed. This source identity replaces the pending commit above. Next action is full immutable exit-cache identity, then remaining approval validation/F evidence corrections; don't repeat the now-completed trailing dispatch fix or mark range behavior accepted.

Exit-cache identity slice: `proactive_exit_research.py` and its tests plus canonical docs/atlas. Freeze supplied bars, bind canonical full effective proposal identity/geometry/clocks, costs and both exit-module/primary-evaluator source digests in versioned manifest. Retain manifest JSON in an additive companion table; legacy rows stay readable but cannot be reused as current evidence. Serialize final recheck/insert, allowing identical concurrent retries without overwriting prior results. Test timing/instrument/implementation/bar/cost changes, actual manifest digest, legacy preservation, identical retry and concurrency. Initial nullable-column approach passed 61 focused tests but review found prior code uses four-value INSERT: abandoned that unshipped approach in favor of a companion table, leaving the original table shape unchanged for rollback compatibility. No strategy/authority/default change; source rollback preserves all rows/companion table. Status IMPLEMENTING; approval validation, range semantics and F evidence remain separate.

Exit-cache verification: Dev engine `.\winvenv\Scripts\python.exe -m pytest tests/test_proactive_exit_research.py tests/test_trailing_stop_profile.py tests/test_proactive_intelligence.py tests/test_proactive_execution_research.py tests/test_proactive_portfolio_research.py tests/test_proactive_market_data.py tests/test_range_reversion_profile.py tests/test_promotion_bridge.py tests/test_legacy_run_id_shim.py tests/test_session_phase_placeholder.py -q -W error`: 137 passed/no warnings in 5.99s. Includes changed identity/clocks/implementation/costs/bars, retained canonical digest, identical/conflicting concurrency, nested bar snapshot, original old-writer table shape and legacy read/preservation tests. Duplicate opportunity IDs rejected before persistence. Root source review complete; independent Terra approves identity/causality, paired-row atomicity and old-schema compatibility without blockers. Its separate 11-file pre-final-duplicate-test rerun included `test_proactive_diagnostics.py` and passed 137 tests in 5.97s; final-source acceptance count above is root's ten-file result, not that distinct scope. Compilation/atlas/diff checks pass, 156 modules. No whole-engine rerun after this cache-only change yet; earlier 2,656-pass receipt predates it. Migration is new companion table only, no result-table ALTER or destructive UPDATE/DELETE. Slice TESTED_DEV pending commit, overall correction plan IMPLEMENTING.

Exit-cache post-commit reconciliation: source `dd60b0c`; immediate seven-file stat/status review confirms clean Dev and matching guide/atlas/plan. Local only, not pushed/deployed. This replaces the pending commit identity above. Next action: approval budget/evidence/expiry validation, preserving operator-only/non-order authority and existing history; accurate F evidence corrections still required.

Approval-budget/validity slice: `promotion_bridge.py` and bridge tests plus canonical documentation/atlas. Require relevant amount, drawdown and integer expiry for approved records; reject missing/nonfinite/bool/wrong-type values. An unsigned record can explicitly retain predeclared budget fields; approval transition validates those immutable fields instead of silently approving with no budget. Validity begins at the retained original decision clock, so a later approval cannot extend that budget window. Read adds separate budget/expiry/version status while retaining recorded state/history, and explicitly reports approval unusable because frozen held-out/account/F/D evidence is not yet validated. No fake gate evidence or operator signature invented. No schema/flags/orders/transport change; rollback source preserves all rows. Test missing fields, malformed values, exact expiry, stale versions, transition rejection preserving state, valid predeclared budgets and historical unbudgeted approval reads. Status IMPLEMENTING. This is not full approval evidence acceptance: the genuine predeclared G/C report protocol and F/D validation still must land before any usable approval.

Approval verification: from Dev `python-engine`, `.\winvenv\Scripts\python.exe -m pytest tests/test_promotion_bridge.py tests/test_proactive_exit_research.py tests/test_trailing_stop_profile.py tests/test_proactive_intelligence.py tests/test_proactive_execution_research.py tests/test_proactive_portfolio_research.py tests/test_proactive_market_data.py tests/test_range_reversion_profile.py tests/test_legacy_run_id_shim.py tests/test_session_phase_placeholder.py tests/test_F_phase1_inventory.py tests/test_cost_schedules.py -q -W error`: **180 passed/no warnings in 6.60s**, exit0. Includes whitespace signer rejection and historical signatures outside the original window. Independent Terra approves final snapshot with no blocker; its bridge-only `-q -W error` rerun passes **65 tests in 1.25s**. Compilation, diff check and atlas regeneration pass (156 modules). No whole-engine rerun after cache/budget yet. Local Dev only; full evidence gate acceptance remains unfinished, not replaced with operator booleans.
