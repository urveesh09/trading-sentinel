# D — release baseline correction

Problem and impact: 17 repository failures prevent release acceptance despite the focused A/B/C checks. Separate stale fixtures and platform encoding from runtime defects; never weaken ledger provenance or delivery authority to make tests green.

Authoritative evidence: source-final C run recorded 2,526 passes, three skips and 17 failures. Dev branch only; Production is read-only and no release is authorized by this slice.

Affected contracts/files: ledger initialization and its legacy test fixtures; declared partner-bot default and disabled gate tests; UTF-8 Python source inspection in penny/scheduler/family tests. Runtime changes require further explicit evidence and plan revision.

Steps: independently review ledger setup; apply real migration in outdated fixtures; align default assertion with declared configuration while retaining explicit disabled-gate checks; use explicit UTF-8 and closed file handles for source reads; run focused and whole-engine acceptance. Investigate warning-fatal Windows socket evidence without suppression. Then review gateway/dashboard/agent and migration/rollback release requirements.

Acceptance: affected groups pass; complete Python comparison has no hidden exclusions; failures/warnings recorded exactly. Migration regression must demonstrate legacy rows preserved and origin_ref available after initialization. No broker calls, messages or feature switches.

Data/config migration: none intended; tests use temporary databases. Existing runtime migration remains authoritative. Rollback: revert this bounded Dev commit, preserving all persistent archives/databases. Promotion only through GitHub, separately verified.

Discovery: UTF-8 inspection reveals eight newer scheduler wrappers missing documented gate delegation/exemptions, not eight demonstrated runtime failures. Inspect actual callees and document verified delegation for advisory entry, research collection and daily summaries. Preserve deliberate off-session lifecycle retirement, delivery recovery and source-neutral reconciliation; document these exceptions rather than preventing cleanup. The SHADOW consumer now supports a validated completed-bar provider as well as fixtures: correct its obsolete fixture-only scheduler description; it has no execution/delivery authority. Momentum's synchronous helper is also used inside async tests, so it mirrors the migrated origin_ref column instead of nesting asyncio.run; the dedicated migration regression and division/audit fixtures exercise real migration.

Status: bounded baseline correction TESTED_DEV in `f7e33cb`; overall D remains IMPLEMENTING. Immediately post-commit inspected `git show --stat --oneline HEAD` and clean Dev status (ahead origin 14). Guide, requirement matrix and checklist match the actual test-only/comment-only runtime diff; atlas regenerated unchanged (155 modules). This docs-only reconciliation records the source commit before further implementation.

Verification (Dev, September 13): affected nine-file test group 148 passed/one skipped/one existing Starlette deprecation in 7.85s. Full engine `winvenv\Scripts\python.exe -m pytest tests -q --junitxml=C:/Users/Urveesh/AppData/Local/Temp/sentinel-d-baseline-20260913.xml`: 2,545 passed/three skipped/23 existing Starlette-httpx deprecations in 126.27s. Scheduler/isolation five-file group with `-W error::RuntimeWarning`: 41 passed/one existing Starlette deprecation in 5.61s. Dashboard `npm.cmd run test:unit` 25 passed and `npm.cmd run build` passed (Node v24.15.0, Vite v5.2.0, existing Browserslist warning). Agent: `docker run --rm --network none --entrypoint python --mount type=bind,source=<Dev>/agent,target=/app,readonly trading-sentinel-agent-test:latest -m pytest tests -q -p no:cacheprovider`: 91 passed in 1.82s, image 6e5d74d56e3f, current source read-only.

Gateway acceptance: node:20-alpine image fb4cd12c85ee, isolated temporary source copy excluding node_modules/.env/data, `npm ci --no-audit --no-fund`, disconnect bridge, then `npm test`: 318 passed/four skipped (25 suites passed/one skipped) in 17.739s. Log retained at `C:/Users/Urveesh/AppData/Local/Temp/sentinel-d-gateway-20260913.log`. Existing two open handles (token-restore timeout on fetch rejection and fake-token Telegram polling in alert-dead-letter tests) still require correction/review; forceExit is the existing script, not newly introduced. Dependency install deprecations were not hidden. The temporary container was removed after its log was preserved; no operational volumes were attached or removed. Atlas regenerated (155 modules), changed-file compileall and diff check pass.

Independent review: Terra confirms normal legacy origin_ref migration works and identified the stale fixtures; Luna applied bounded explicit UTF-8/closed-handle test fixes, locally rechecked by root. Migration's broad exception swallowing is a separately identified resilience concern, not changed here. Combined warning-fatal socket root-cause investigation continues. No runtime behavior, accounting/default values, schedules or delivery permission changed; comments reconcile actual contracts. No push/deployment or actual message/order. D remains IMPLEMENTING, not RELEASE_VALIDATED.

Remaining: clean release suite, live release fingerprints, real session coverage and adequate held-out evidence; E–J remain independently required. Exact next action: inspect and correct the documented test setup drift, then run the full Python suite.
