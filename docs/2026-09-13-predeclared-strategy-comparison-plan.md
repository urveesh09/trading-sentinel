# G/C — predeclared strategy comparison protocol

Problem: existing matched entry/exit reports evaluate every profile retrospectively; chronological folds select policies but do not retain a predeclared basket/criteria frozen before holdout. G gap4 remains open. C now has explicit disjoint nonempty chronological session rules and immutable criteria. Add an offline protocol using those same session-order invariants, not a substitute threshold strategy or live consumer.

Files/contracts: new `python-engine/proactive_comparison_protocol.py` and focused tests; canonical guide/atlas/plan/checklist updated by root. Preserve existing simulator/report signatures, existing result tables/history and C evaluator behavior. Explicit protocol includes account/id/code/cost snapshot, training/holdout sessions, frozen time, bounded named entry/exit alternatives, baseline, cash/cost stress and research acceptance thresholds. No broad variant search; no silent winner chosen after holdout.

Operator entrypoint slice (root): `research_cli.py` and new CLI tests expose `freeze-strategy-comparison`/`evaluate-strategy-comparison`, explicit offline `--db` and JSON input paths, no operational DB default and no backdating `--now` switch. Parse proposal clocks as aware datetimes and retain full output atomically without overwrite; identical retry only. No scheduler/qualification/order consumer. Root and Terra have distinct file ownership; Terra owns protocol/test module only. Existing CLI behavior preserved. Tests check explicit inputs, full proposal/coverage forwarding, no caller-clock override and immutable output failure/retry.

Implementation: validate/freeze manifest; persist immutable protocol before first holdout session under actual/injected aware clock; exact reuse only. Evaluate matched proposals using actual simulator for each declared alternative and baseline/stress coordinates. Retain every declared session including zero-opportunity/missing input, all no-fill/open/invalid states, independent opportunity count, sequential realised drawdown, frequency/turnover/capacity and paired baseline difference with uncertainty. Research disposition REJECTED/UNCERTAIN/SUPPORTS_FURTHER_RESEARCH explains gates; never promotes live. Input self-hashes establish integrity only, not independent provenance. Scope cross-session/overlapping exposure limitations honestly; don't claim independent sample from variants.

Acceptance: chronological/disjoint/ISO sessions, freeze-before-holdout IST boundary and future timestamp rejection, changed criteria/cost/basket identity rejection; matching profile inputs and stress, zero/missing sessions, duplicate opportunities, no-fill/open preservation, causal closes/drawdown, uncertainty and no authority. Persist canonical manifest plus report with atomic immutable/idempotent write and history retention. Independent root review/reproduction and focused/full relevant regression before commit.

Migration/rollback: new offline tables only; no ALTER/UPDATE/DELETE of operational stores, flags, scheduling, broker/Telegram transport or approved budgets. Rollback source through GitHub preserves all records. Dev only. Real heldout acquisition/F reconciliation/D observation/operator risk input still needed; this protocol cannot manufacture them. Status IMPLEMENTING.

Fresh operational observation at2026-09-13 06:45:28UTC: read-only `docker ps -a` and `docker inspect python-engine node-gateway agent` show all application containers exited137/OOMKilledfalse, original finish clocks01:01:23–26UTC; nginx restarting, ngrok/autoheal running. Production tracked checkout is release954e25a; prior untracked audit/migration artifacts preserved. No restart/stop/edit performed. Real collection remains unavailable; no authority inferred from stopped state. CLI transport tests are synthetic contract tests only (five pass with warnings fatal0.57s); actual evaluator and retained comparison still require combined acceptance.

Supervision discoveries: initial protocol draft had opaque thresholds, point-value pseudo-bootstrap, only baseline disposition, clock-dependent freeze retries and dropped cost metadata. Root required executable explicit gates, actual seeded session resampling including closed/no-fill paired differences, per-alternative baseline/stress decisions, full cost snapshot binding and canonical idempotent retries. Both protocol/evaluator source identities and stored hashes must be checked. Future observations cannot become already-evaluated outcomes. RANGE_REVERSION's confirmation alias remains labeled unsupported, not a faithful range result. These are unaccepted draft defects, not shipped behavior.

CLI checks: `research_cli.py freeze-strategy-comparison --help` and `evaluate-strategy-comparison --help` return0 with required explicit paths and no `--now`. Root transport tests plus existing qualification CLI: six pass/no warnings0.57s after concurrent immutable-output regression. An attempted broader command referenced nonexistent `test_research_cli.py`/`test_research_cli_full_policy.py` and ran no tests; `rg --files` confirms the actual qualification CLI file. Actual protocol/CLI integrated test is authored but acceptance awaits evaluator completion.

Operator commands from Dev `python-engine` (offline paths chosen explicitly; no runtime DB default):

```powershell
.\winvenv\Scripts\python.exe research_cli.py freeze-strategy-comparison --db C:/research/comparison.db --manifest C:/research/protocol.json --output C:/research/frozen.json
.\winvenv\Scripts\python.exe research_cli.py evaluate-strategy-comparison --db C:/research/comparison.db --protocol-id declared-basket --report-id heldout-1 --inputs C:/research/inputs.json --output C:/research/heldout-1.json
```

Manifest keys: protocol_id/account_id/code_revision/frozen_at, training_sessions/holdout_sessions ISO dates, alternatives `{name,entry,exit}`, baseline alternative name, cash, complete cost_snapshot (fee_rate/slippage_bps plus declared schedule provenance), cost_stress `{fee_multiplier,additional_slippage_bps}`, thresholds `{minimum_closed_outcomes,minimum_complete_sessions,maximum_drawdown_pct,minimum_net_expectancy,minimum_paired_delta,confidence_level,bootstrap_samples}`. Inputs object contains complete ShadowProposal dictionaries with aware ISO clocks, future_bars mapping and session_coverage `{alternative:{session:"COMPLETE"|"MISSING"}}`. COMPLETE is a caller coverage claim, not independent source authenticity. Retain no-opportunity sessions; report missing before collection rather than manufacturing a passing result.
