# I — immutable annotation inputs and original validity

Problem: root's offline controlled-clock reproduction on Dev `9e35bcf` obtains READY after original expiry, then CACHED by resubmitting the same key with a later deadline. `_Task` also shallow-copies nested signal input. A stale annotation must not appear current or describe a subsequently mutated decision.

Files/contracts: `agent/async_reviews.py` and `agent/tests/test_async_reviews.py`; guide/atlas/plan/checklist maintained by root. External agent owns F/G and cost schedules: leave those source files and their documents unchanged. Existing reviewer signature, transport isolation, durable budget, queue/circuit bounds and nonblocking deterministic paths remain unchanged.

Steps: deep snapshot reviewed nested signal inputs before queue admission; retain original validity for returned READY/CACHED states; expire status and cache at the earlier of original deadline and cache TTL. Cached-request validity can tighten but never extend the original deadline. Exact deadline is elapsed (`>=`); discard results completing at that instant. Add controlled-clock regressions for expiry/status/later resubmission, exact completion, nested mutation and cache TTL. Reviewer input types/signature remain unchanged; no model requests or operational data.

Acceptance: original reproduction cannot return an expired READY/CACHED review; nested caller mutation cannot change reviewer input. Existing disabled, queue saturation, provider failure, budget and restart tests remain green. Full current agent suite in isolated network-disabled runtime, no credentials or Production volume. Preserve bounded state retention and don't leak worker threads.

Verification: final-source Windows `.\python-engine\winvenv\Scripts\python.exe -m pytest agent/tests/test_async_reviews.py -q -W error` from Dev returns0: 14 passed/no warnings in 1.14s. Final isolated Linux agent suite 98 passed with all warnings fatal in 2.10s, cached `trading-sentinel-agent-test:latest`, current source read-only, `--pull=never --network none`, no credentials or operational volume. All test workers shut down in finally. Compilation/diff checks pass; AST atlas regenerated at 156 modules, including the other agent's new promotion bridge. Root reviewed the exact source diff; original controlled-clock reproduction establishes the defect rather than merely testing expected status names.

Migration/config: none; optional annotation lifecycle only. Rollback through reviewed GitHub source revert, retain durable budgets and all data. No activation or transport authorization. Status TESTED_DEV in source commit `59e915a`; immediate seven-file commit stat/status review confirms clean Dev and matching documentation. Local only, not pushed/deployed. Overall I IMPLEMENTING. This is one I slice, not complete sourced-news/model/prompt provenance or annotation usefulness acceptance. D/E/H/J and genuine release/session/review evidence remain required; F/G belong to the parallel agent, whose correctness audit the user requested explicitly.
