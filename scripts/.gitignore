# Audit-script output artifacts (regenerated on demand).
# These are produced by scripts/audit_*.py and scripts/build_release_notes.py
# for the operator's review; they should not be committed.

# D.2.a-migrations output
/migrations.json
/defaults.json

# D.3 release notes output (regenerated by build_release_notes.py)
/RELEASE_NOTES.md

# Per-test-run fixtures left behind in scripts/tests/
/tests/*.json
/tests/RELEASE_NOTES.md
